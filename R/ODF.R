###combine ODF###

#'
#' This function should be able to combine individual 'bin' ODF files
#' from an ADCP profile into a single adp object (in oce)
#' It needs to read in data and metadata from ODFs
#' It needs to order bins appropriately
#' It needs to check for missing depth bins and insert placeholders (empty bin)
#'



#list.odf<- function(directory, extension){
 # od <- list.files(path = dircetory, pattern = extension "*.ODF")
  #list()
  #for(k in 1:length(od)){
   # list[[k]] <- read.odf(od[k])
  #}
#}

